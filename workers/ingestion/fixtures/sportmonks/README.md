# Sportmonks HTTP canned responses

These files are **test doubles** of Sportmonks Football API v3 JSON.
They are not product facts and must never be served with `data_mode=live`
outside automated tests.

They contain no API tokens.
