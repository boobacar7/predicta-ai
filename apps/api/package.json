{
  "name": "predicta-api",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --port 8000",
    "test": "python -m pytest",
    "lint": "python -m ruff check app tests",
    "typecheck": "python -m mypy",
    "verify": "python -m ruff check app tests && python -m mypy && python -m pytest"
  }
}
