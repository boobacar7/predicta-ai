# Data access mode. Prototype defaults to mock fixtures.
# Use `http` only after the generated API client is wired by the Frontend agent.
NEXT_PUBLIC_PREDICTA_DATA_SOURCE=mock

# Production builds refuse mock data unless this flag is explicitly true.
NEXT_PUBLIC_PREDICTA_ALLOW_MOCK_IN_PROD=false
